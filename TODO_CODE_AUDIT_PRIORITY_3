Based on my analysis of both the `COMPREHENSIVE_CODE_AUDIT.md` and TODO_CODE_AUDIT files, here's the outline of next steps required to complete **Priority 3 - Code Organization**:

## **Priority 3 - Code Organization: Actionable Next Steps**

### **1. Module Structure Improvements**

#### **1.1 Extract Common Event Handling Patterns**
- **Create `utils/EventUtils.js`** - Extract common event listener setup/cleanup patterns
  - `createEventHandler()` - Standardizes event handler creation with error handling
  - `setupEventListeners()` - Batch setup with automatic cleanup tracking
  - `cleanupEventListeners()` - Centralized cleanup utility
- **Refactor map.js event setup** - Replace 50+ individual `addEventUnsubscriber()` calls with batch setup
- **Update all handlers** - RouteEditHandler, RouteManager, etc. to use new utilities

#### **1.2 Consolidate State Manager Patterns** ✅ COMPLETED
- **Create `state/BaseStateManager.js`** - Abstract base class for all state managers
  - Standard `_emitChange()` implementation
  - Consistent `saveToStorage()`/`loadFromStorage()` methods
  - Unified error handling integration
- **Refactor all state managers** to extend BaseStateManager:
  - ✅ MapState.js - Removed duplicate code, inherits common patterns
  - ✅ SelectionState.js - Simplified constructor, inherits event emission
  - ✅ EditModeState.js - Standardized with base class patterns
  - ✅ RouteState.js - Unified with common state manager interface
  - ✅ HighlightState.js - Refactored to use base class
  - ✅ TilesetState.js - Simplified constructor and removed duplicate methods
  - ✅ RouteAnimationState.js - Inherits standardized event emission
  - ✅ RouteEditState.js - Updated constructor to match base class pattern
  - ✅ DragState.js - Refactored to use base class inheritance
  - ✅ LayerState.js - Unified with common patterns (preserved layerKeys parameter)
  - ✅ HeatmapDisplayState.js - Simplified to use base class
  - 🔄 ImageState.js - Left unchanged (special case for image management)
- **Standardize missing methods** - All managers already had storage methods or appropriate no-ops

#### **1.3 Improve Import/Export Patterns**
- **Convert IIFE modules to ES6 modules** - Where appropriate for better tree-shaking
- **Create index files** - `state/index.js`, `rendering/index.js`, etc. for cleaner imports
- **Remove global pollution** - Replace `window.*` assignments with proper exports

### **2. Code Deduplication**

#### **2.1 Remove Duplicate Route Calculations** ✅ COMPLETED
- **Consolidate route length calculations** - Single source of truth in `RouteManager.computeRouteLengthNormalized()`
- **Split legacy utilities** into pure utilities (`RouteMath`) and stateful operations (`RouteManager`) — completed

#### **2.2 Standardize Event Listener Patterns** ✅ COMPLETED
- **Extract common listener setup** - All modules now use standardized pattern:
  ```javascript
  const eventManager = EventUtils.createEventManager(this);
  eventManager.setup(eventBus, [...listeners...], this, errorHandler);
  ```
- **Create standardized pattern** with consistent error handling and cleanup ✅
- **Status:** `utils/EventUtils.js` implemented and included; all production modules migrated:
  - ✅ `data/MarkerManager.js` - Uses `EventUtils.createEventManager(this)` with fallback
  - ✅ `controllers/SettingsController.js` - Uses `EventUtils.setupEventListeners()` with fallback
  - ✅ `data/RouteManager.js` - Uses `EventUtils.createEventManager(this)`
  - ✅ `map.js` - Uses `EventUtils.createEventManager(window, '_pendingEventUnsubscribers')` for 20+ listeners; cleanup in `destroy()`
  - Direct `eventBus.on()` calls are **intentional fallbacks** when EventUtils unavailable at runtime
  - `EventUtils.createEventManager(target)` provides `setup()` and `cleanup()` helpers
  - `EventUtils.setupEventListeners(eventBus, listeners, context, errorHandler)` wraps handlers with error handling and returns unsubscribe functions

**Remaining work (optional enhancement):**
- Add unit tests to verify listener cleanup on component `destroy()` paths for `RouteManager`, `MarkerManager`, `SettingsController`, and `map` (currently working but untested)

#### **2.3 Extract Common Rendering Utilities** ✅ COMPLETED
- **Create `rendering/RenderUtils.js`** - Common canvas operations
  - ✅ Color conversion utilities (`hexToRgba` delegates to ColorUtils; includes fallback)
  - ✅ Coordinate transformation helpers (`worldToScreen`)
  - ✅ Common shape drawing functions (`drawCircle`, `drawRoundedRect`)
- **Refactor renderers** to use shared utilities instead of duplicated code
  - ✅ `rendering/RouteRenderer.js` - Now delegates color conversion to RenderUtils with safe fallbacks
  - ✅ `controllers/LayerListController.js` - `_colorToRgba()` now tries RenderUtils/ColorUtils first, then manual parsing
  - ✅ `map.js` tooltip color - Uses RenderUtils.hexToRgba with ColorUtils and legacy fallbacks
  - ✅ Edit mode functions (`updateEditOverlay`, `setEditToggleColor`) - Intentionally left isolated (UI state management coupling, not rendering utils)
- **Removed Markdown fences** from `rendering/utils/colorUtils.js` and `rendering/RenderContext.js` for valid JavaScript

### **3. Documentation and Naming**

#### **3.1 Add Comprehensive JSDoc Comments**
- **Document all public APIs** - Every exported class, method, and function
- **Standardize parameter documentation** - Use consistent `@param` format
- **Add `@returns` and `@throws`** - For all methods that return values or can throw
- **Document event contracts** - As mentioned in audit, add JSDoc for expected event data formats

#### **3.2 Improve Variable and Function Naming**
- **Standardize naming conventions**:
  - Private methods: `_methodName()` (already mostly consistent)
  - Event handlers: `_onEventName()` 
  - State properties: `camelCase` for public, `_underscore` for private
- **Rename confusing variables** - e.g., `map` parameter in RouteRenderer constructor vs `this.map`

#### **3.3 Create API Documentation**
- **Generate API docs** - Use JSDoc to generate HTML documentation
- **Create `docs/API.md`** - High-level API overview for developers
- **Document state manager interfaces** - Clear contracts for what each manager provides

### **4. Clean Up Technical Debt (From Audit)**

#### **4.1 Remove Dead Code**
- **Clean up unused EventTypes** - Remove events that are defined but never emitted:
  - `RENDER_COMPLETED`, `RENDERER_*_UPDATED`, `INPUT_*` events, etc.
- **Remove RouteState if unused** - ✅ COMPLETED: Migrated all callers to RouteManager, removed script include, deleted file and test
- **Remove duplicate state properties** - Like `_routeNodeCandidate` in RouteEditHandler ✅ COMPLETED: Removed all _routeNodeCandidate duplication - RouteEditHandler, InteractiveMap, and RouteComputation now use `DragState.routeNodeCandidate` consistently; updated tests

#### **4.2 Fix Remaining Architecture Issues**
- **Fix OverlayRenderer global access** - Replace `window.interactiveMap` with event emission ✅ COMPLETED: OverlayRenderer already uses eventBus.emit for tooltip positioning; no global access found
- **Standardize error handling** - Remove any remaining `console.log/error` calls ✅ COMPLETED: Replaced console.warn/error calls in map.js and index.html with proper errorHandler.logWarn/logDebug calls
- **Fix inconsistent patterns** - Make all handlers follow same constructor/destroy pattern ✅ COMPLETED:
  - ✅ Added destroy() methods to GestureHandler, MarkerEditHandler, MarkerManager, SettingsController
  - ✅ Updated InteractiveMap.destroy() to clean up SettingsController and MarkerManager
  - ✅ All handlers now follow consistent constructor/init/destroy pattern with proper event listener cleanup

### **5. Implementation Priority Order**

1. **High Impact, Low Risk:**
   - Extract event handling utilities
   - Consolidate route length calculations
   - Add JSDoc to core classes (RouteManager, state managers)

2. **Medium Impact, Medium Risk:**
   - Create BaseStateManager and refactor existing managers
   - Standardize naming conventions
   - Remove dead EventTypes

3. **Low Impact, High Risk:**
   - Convert to ES6 modules (requires testing all imports)
   - Major refactoring of event listener patterns

### **6. Success Criteria**

- **Zero duplicate code** for core operations (route calculations, event handling)
- **Consistent patterns** across all modules (state managers, handlers, renderers)
- **Complete JSDoc coverage** for all public APIs
- **Clean architecture** with proper separation of concerns
- **No dead code** or unused event definitions

This would bring the codebase to ~85-90% completion of the audit issues, leaving only Priority 4 technical debt cleanup.